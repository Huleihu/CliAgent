# 开发规则

- 保持模块化：一个模块只负责一类清晰职责，避免万能文件。
- 保持解耦：依赖抽象和明确接口，避免业务逻辑直接绑定基础设施。
- 设计模式按需使用；优先选择简单、可读的实现，避免为模式而模式。
- 为公共接口、关键决策和非显然逻辑写简洁注释；注释解释“为什么”，不重复代码“做什么”。
- 代码中的注释、文档字符串及面向人的异常/日志文本统一使用中文；标识符、第三方协议字段和通用技术术语可按 Python 与生态惯例保留英文。
- 设计时考虑扩展点：配置、工具和运行时能力应可替换、可新增，并保持向后兼容。
- 保持运行时循环通用且精简：章节业务能力优先通过独立领域包、端口与适配器、内置工具和组合根接入；除非现有边界确实无法复用并已先向用户说明证据、获得确认，否则不得将新章节逻辑堆入或重构 `MinimalAgentLoop`、`runtime/loop.py`。
- 每次完成实质性改动后，更新 `HANDOFF.md`，记录当前状态、改动、验证结果和下一步。
- 提交信息使用 Conventional Commits：`type: 简短中文摘要`，空行后用中文项目符号列出主要改动；例如 `feat: 支持多 Agent 协作`。
- 当一个教学式小步已通过相应验证、边界清晰且适合形成独立提交时，主动提醒用户可以提交，并给出建议的 Conventional Commit 信息；除非用户明确要求，不自动执行提交。
- 每个教学式小步完成后，主动给出本次新增或修改文件的推荐阅读顺序：先读领域契约与公共接口，再读实现或适配器，随后读装配/运行时接入，最后读对应测试；每个文件用一句话说明阅读目标及其与下一文件的关系。若本次仅改动单个文件，直接说明其职责即可。
- 教学式小步开发：每次只实现一个可理解、可验证的小步骤，先说明意图与关键取舍，等待用户检查后再继续；避免一次性实现大量功能。开发前先说明设计思路、模块职责和数据流，再写代码。修改已有代码前，先说明原因和影响范围。
- 集成 learnClaudeCode 的一个 S 章节前，先用教学语言说明该章节解决的问题，再基于当前项目分析需求、模块职责、数据流、影响范围与集成方式；随后给出不超过 5 步、每步边界清晰且可验证的实施方案。等待用户确认方案后再写代码；每完成一步均停下说明结果、验证和下一步，不将多个步骤合并为难以理解的大改动。
